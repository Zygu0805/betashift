# AlphaShift - 캐로셀 배정 자동화 시스템

## 📋 프로젝트 개요

**프로젝트명**: AlphaShift  
**목표**: 공항 도착 수하물 캐로셀 배정 자동화 소프트웨어 개발  
**개발 방식**: 바이브 코딩 (Claude Code 활용, 단계별 세분화 진행)

---

## 🛠 기술 스택

| 영역 | 기술 | 비고 |
|------|------|------|
| **프론트엔드** | React (JS → TS) | 초기 JS로 시작, 후반 TypeScript 마이그레이션 |
| **백엔드** | FastAPI (Python) | 비동기 처리 지원 |
| **데이터베이스** | PostgreSQL | 관계형 DB |
| **호스팅** | Heroku 또는 대안 | 무료/저비용 호스팅 |

---

## 🎨 UI 구성

### 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────┐
│ [좌] AlphaShift                    [우] 날짜 선택 + 로드 버튼 │
├─────────────────────────────────────────────────────────────┤
│ [좌] Manual / A.I. 모드 토글 스위치                          │
├─────────────────────────────────────────────────────────────┤
│ 시간 재생 바 (00:00:00 ~ 23:59:59)                          │
│ [◀] [▶ 재생/정지] [▶▶]  배속: 60x / 30x / 20x / 10x         │
├─────────────────────────────────────────────────────────────┤
│                    바차트 형태의 배정 UI                      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ C1  │▓▓▓KE001▓▓▓│    │▓▓OZ123▓▓│                    │   │
│  │ C2  │   │▓▓7C456▓▓│         │▓▓▓▓TW789▓▓▓▓│         │   │
│  │ C3  │▓JL012▓│              │▓▓▓▓▓NH345▓▓▓▓▓│        │   │
│  │ ... │                                                │   │
│  │ C24 │        │▓▓▓▓▓AA678▓▓▓▓▓│                       │   │
│  └──────────────────────────────────────────────────────┘   │
│       00  01  02  03  04  05  06  07  08  ... 22  23        │
│                         │ (현재 시각 커서)                   │
└─────────────────────────────────────────────────────────────┘
```

### 주요 UI 요소

1. **헤더**
   - 좌측: 시스템명 "AlphaShift"
   - 우측: 날짜 선택 Input + 데이터 로드 버튼

2. **모드 전환**
   - Manual / A.I. 토글 스위치

3. **시간 재생 컨트롤**
   - 타임라인 바: 00:00:00 ~ 23:59:59
   - 재생/정지 버튼
   - 배속 선택: 60x (기본) / 30x / 20x / 10x

4. **바차트 (배정 현황)**
   - 가로축: 시간대 (1시간 단위)
   - 세로축: 캐로셀 번호 (C1 ~ C24)
   - 항공편: 항공사별 색상 구분된 바(bar)
   - 바 길이: 캐로셀 점유 예정 시간

---

## ⚙️ 기능 요구사항

### 1. 바차트 인터랙션

| 기능 | 설명 |
|------|------|
| **드래그앤드롭** | Manual 모드에서 항공편 바를 다른 캐로셀로 이동 가능 |
| **드롭 피드백** | 드롭 전 해당 캐로셀 행 테두리가 빛나는 효과 |
| **상세 정보** | 바 클릭 시 항공편 상세 정보 표시 (편명, 항공사, 도착시간 등) |
| **충돌 표시** | 겹침/충돌 시 바 중앙 상단에 ⚠️ 배지 표시 |

### 2. 시간 재생 기능

| 기능 | 설명 |
|------|------|
| **배속 조절** | 60x, 30x, 20x, 10x (실시간 대비) |
| **타임라인 동기화** | 재생 중 바차트에 세로선(현재 시각 커서)이 이동 |
| **커서 조작** | 타임라인 커서 수동 조작 시 바차트 동기화 |

### 3. A.I. 모드

| 기능 | 설명 |
|------|------|
| **충돌 회피 배정** | 항공편 간 충돌 발생 시 빈 캐로셀 자동 탐색 및 배정 |
| **균등 분배** | 캐로셀 사용률 균등화 |
| **수동 조정** | AI 배정 후 Manual 모드에서 수동 조정 가능 |

### 4. 캐로셀 제약사항

- 현재 단계에서는 제약사항 미적용 (추후 확장 예정)

---

## 🗄 데이터베이스 스키마

### 테이블 구조

#### flights (항공편)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| flight_id | VARCHAR(20) PK | 고유 식별자 |
| airline | VARCHAR(10) | 항공사 코드 (KE, OZ, 7C 등) |
| flight_number | VARCHAR(10) | 편명 |
| scheduled_time | TIMESTAMP | 도착 예정 시간 |
| pax_count | INTEGER | 승객 수 |
| baggage_count | INTEGER | 수하물 개수 |
| aircraft_type | VARCHAR(20) | 항공기 종류 |
| created_at | TIMESTAMP | 생성 시간 |

#### carousels (캐로셀)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| carousel_id | VARCHAR(10) PK | 캐로셀 ID (C1~C24) |
| terminal | VARCHAR(10) | 터미널 정보 |
| capacity | INTEGER | 용량 |
| is_active | BOOLEAN | 운영 상태 |

#### assignments (배정)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| assignment_id | SERIAL PK | 배정 ID |
| flight_id | VARCHAR(20) FK | 항공편 ID |
| carousel_id | VARCHAR(10) FK | 캐로셀 ID |
| start_time | TIMESTAMP | 점유 시작 시간 |
| end_time | TIMESTAMP | 점유 종료 시간 |
| assignment_type | VARCHAR(10) | 배정 타입 (MANUAL/AI) |
| created_at | TIMESTAMP | 생성 시간 |
| updated_at | TIMESTAMP | 수정 시간 |

#### airlines (항공사)

| 컬럼 | 타입 | 설명 |
|------|------|------|
| airline_code | VARCHAR(10) PK | 항공사 코드 |
| airline_name | VARCHAR(100) | 항공사명 |
| color_code | VARCHAR(7) | UI 표시 색상 (#RRGGBB) |

---

## 🔌 API 명세

### 항공편 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/flights` | 항공편 목록 조회 |
| GET | `/api/flights?date={YYYY-MM-DD}` | 특정 날짜 항공편 조회 |
| GET | `/api/flights/{flight_id}` | 항공편 상세 조회 |
| POST | `/api/flights/upload` | 항공편 데이터 업로드 (JSON) |

### 캐로셀 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/carousels` | 캐로셀 목록 조회 |
| GET | `/api/carousels/{carousel_id}` | 캐로셀 상세 조회 |

### 배정 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/assignments?date={YYYY-MM-DD}` | 특정 날짜 배정 현황 조회 |
| POST | `/api/assignments` | 배정 생성 |
| PUT | `/api/assignments/{assignment_id}` | 배정 수정 (수동 변경) |
| DELETE | `/api/assignments/{assignment_id}` | 배정 삭제 |
| POST | `/api/assignments/ai-assign` | AI 자동 배정 실행 |
| POST | `/api/assignments/validate` | 충돌 검증 |

### 항공사 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/airlines` | 항공사 목록 (색상 정보 포함) |

---

## 📅 개발 로드맵 (Step-by-Step)

---

### Phase 1: Backend 환경설정 및 DB 구축

#### Step 1.1: PostgreSQL 설정
- [x] PostgreSQL 설치 (v17.6)
- [x] 데이터베이스 생성 (`betashift`)
- [ ] 사용자 생성 및 권한 부여
- [x] 접속 테스트 (psql)

#### Step 1.2: FastAPI 프로젝트 초기화
- [x] backend 폴더 생성
- [x] Python 가상환경 생성 및 활성화 (conda: betashift, Python 3.11)
- [x] 필수 패키지 설치 (`fastapi`, `uvicorn`, `sqlalchemy`, `psycopg2-binary`, `python-dotenv`)
- [x] `requirements.txt` 작성
- [x] 기본 `main.py` 작성
- [x] 서버 실행 확인 (`uvicorn app.main:app --reload`)

#### Step 1.3: 프로젝트 구조 설정
- [x] 폴더 구조 생성 (`app/`, `models/`, `schemas/`, `routers/`, `services/`)
- [x] `database.py` 작성 (DB 연결 설정)
- [x] `.env` 파일 생성 (DB 접속 정보)
- [x] DB 연결 테스트

#### Step 1.4: 테이블 생성 (Models)
- [ ] `models/flight.py` - Flight 모델
- [ ] `models/carousel.py` - Carousel 모델
- [ ] `models/assignment.py` - Assignment 모델
- [ ] `models/airline.py` - Airline 모델
- [ ] 테이블 자동 생성 확인

---

### Phase 2: 기본 CRUD API 구현

#### Step 2.1: Pydantic 스키마 정의
- [ ] `schemas/flight.py` - Flight 스키마
- [ ] `schemas/carousel.py` - Carousel 스키마
- [ ] `schemas/assignment.py` - Assignment 스키마
- [ ] `schemas/airline.py` - Airline 스키마

#### Step 2.2: 항공편 API
- [ ] `routers/flights.py` 생성
- [ ] GET `/api/flights` - 목록 조회
- [ ] GET `/api/flights/{flight_id}` - 상세 조회
- [ ] POST `/api/flights` - 생성
- [ ] POST `/api/flights/upload` - JSON 벌크 업로드
- [ ] Swagger UI에서 테스트

#### Step 2.3: 캐로셀 API
- [ ] `routers/carousels.py` 생성
- [ ] GET `/api/carousels` - 목록 조회
- [ ] GET `/api/carousels/{carousel_id}` - 상세 조회
- [ ] POST `/api/carousels/init` - 초기 데이터 생성 (C1~C24)

#### Step 2.4: 항공사 API
- [ ] `routers/airlines.py` 생성
- [ ] GET `/api/airlines` - 목록 조회 (색상 정보 포함)
- [ ] POST `/api/airlines` - 생성

#### Step 2.5: 배정 API (기본)
- [ ] `routers/assignments.py` 생성
- [ ] GET `/api/assignments` - 목록 조회 (날짜 필터)
- [ ] POST `/api/assignments` - 배정 생성
- [ ] PUT `/api/assignments/{id}` - 배정 수정
- [ ] DELETE `/api/assignments/{id}` - 배정 삭제

---

### Phase 3: 배정 로직 구현

#### Step 3.1: 충돌 검증 로직
- [ ] `services/assignment_service.py` 생성
- [ ] 시간 겹침 검증 함수 구현
- [ ] 동일 캐로셀 충돌 체크

#### Step 3.2: 배정 검증 API
- [ ] POST `/api/assignments/validate` - 충돌 검증
- [ ] 충돌 시 상세 정보 반환 (어떤 항공편과 충돌하는지)

#### Step 3.3: 수동 배정 완성
- [ ] 배정 생성 시 충돌 검증 적용
- [ ] 배정 수정 시 충돌 검증 적용
- [ ] 충돌 허용 옵션 (경고만 표시)

---

### Phase 4: AI 배정 알고리즘

#### Step 4.1: 기본 자동 배정
- [ ] `services/ai_assignment_service.py` 생성
- [ ] 빈 캐로셀 탐색 알고리즘
- [ ] 시간대별 캐로셀 사용 현황 조회

#### Step 4.2: 충돌 회피 배정
- [ ] 충돌 발생 시 대안 캐로셀 탐색
- [ ] 앞뒤 항공편과 충돌 없는 캐로셀 선택

#### Step 4.3: 균등 분배 알고리즘
- [ ] 캐로셀별 사용률 계산
- [ ] 사용률 낮은 캐로셀 우선 배정
- [ ] 균등 분배 가중치 적용

#### Step 4.4: AI 배정 API
- [ ] POST `/api/assignments/ai-assign` - 전체 자동 배정
- [ ] POST `/api/assignments/ai-assign/{flight_id}` - 단일 항공편 자동 배정

---

### Phase 5: Frontend 환경설정

#### Step 5.1: React 프로젝트 초기화
- [ ] frontend 폴더 생성
- [ ] Vite로 React 프로젝트 생성 (`npm create vite@latest`)
- [ ] 필수 패키지 설치 (`axios`, `react-router-dom` 등)
- [ ] 개발 서버 실행 확인

#### Step 5.2: 프로젝트 구조 설정
- [ ] 폴더 구조 생성 (`components/`, `pages/`, `hooks/`, `services/`, `styles/`)
- [ ] API 서비스 설정 (`services/api.js`)
- [ ] 기본 스타일 설정 (CSS 또는 Tailwind)

#### Step 5.3: 기본 레이아웃 구현
- [ ] `App.jsx` - 메인 레이아웃
- [ ] `components/Header.jsx` - 상단 헤더 (로고, 날짜 선택, 로드 버튼)
- [ ] `components/ModeToggle.jsx` - Manual/AI 토글
- [ ] `components/TimelineControl.jsx` - 시간 재생 컨트롤

---

### Phase 6: 바차트 시각화

#### Step 6.1: 바차트 기본 구조
- [ ] `components/GanttChart.jsx` - 바차트 컨테이너
- [ ] 가로축 (시간) 렌더링
- [ ] 세로축 (캐로셀 C1~C24) 렌더링
- [ ] 그리드 라인 표시

#### Step 6.2: 항공편 바 렌더링
- [ ] `components/FlightBar.jsx` - 항공편 바 컴포넌트
- [ ] 시간 기반 위치/길이 계산
- [ ] 항공사별 색상 적용

#### Step 6.3: 데이터 연동
- [ ] Backend API 연결
- [ ] 날짜별 데이터 로드
- [ ] 로딩/에러 상태 처리

---

### Phase 7: 인터랙션 구현

#### Step 7.1: 바 클릭 - 상세 정보
- [ ] 클릭 이벤트 핸들링
- [ ] `components/FlightDetail.jsx` - 상세 정보 팝업
- [ ] 편명, 항공사, 도착시간, 수하물 수 등 표시

#### Step 7.2: 드래그앤드롭
- [ ] 드래그 시작/진행/종료 이벤트
- [ ] 드롭 대상 캐로셀 행 하이라이트 (테두리 빛남 효과)
- [ ] 드롭 시 배정 변경 API 호출

#### Step 7.3: 충돌 표시
- [ ] 충돌 검증 API 연동
- [ ] 충돌 시 ⚠️ 배지 표시
- [ ] 충돌 상세 정보 툴팁

---

### Phase 8: 시간 재생 기능

#### Step 8.1: 타임라인 구현
- [ ] 타임라인 바 UI
- [ ] 현재 시각 표시
- [ ] 클릭/드래그로 시간 이동

#### Step 8.2: 재생 기능
- [ ] 재생/정지 버튼
- [ ] 배속 선택 (60x/30x/20x/10x)
- [ ] setInterval 기반 시간 진행

#### Step 8.3: 바차트 동기화
- [ ] 현재 시각 커서 (세로선) 표시
- [ ] 타임라인-바차트 양방향 동기화

---

### Phase 9: TypeScript 마이그레이션

#### Step 9.1: 설정
- [ ] TypeScript 설치 및 설정 (`tsconfig.json`)
- [ ] 파일 확장자 변경 (`.jsx` → `.tsx`)

#### Step 9.2: 타입 정의
- [ ] `types/flight.ts` - Flight 타입
- [ ] `types/carousel.ts` - Carousel 타입
- [ ] `types/assignment.ts` - Assignment 타입
- [ ] API 응답 타입 정의

#### Step 9.3: 컴포넌트 마이그레이션
- [ ] 컴포넌트별 Props 타입 적용
- [ ] 훅 타입 적용
- [ ] 타입 에러 해결

---

### Phase 10: 배포

#### Step 10.1: 배포 플랫폼 선정
- [ ] Heroku / Railway / Render 등 비교
- [ ] 플랫폼 결정 및 계정 생성

#### Step 10.2: Backend 배포
- [ ] `Procfile` 작성 (Heroku의 경우)
- [ ] 환경변수 설정
- [ ] DB 연결 설정 (Production)
- [ ] 배포 및 테스트

#### Step 10.3: Frontend 배포
- [ ] 빌드 설정
- [ ] 정적 파일 배포 (Vercel / Netlify 등)
- [ ] API 엔드포인트 설정

#### Step 10.4: 통합 테스트
- [ ] 전체 기능 테스트
- [ ] 성능 확인
- [ ] 버그 수정

---

## 📌 TODO (다음 작업)

> **마지막 작업일**: 2025-12-14

### 다음에 이어서 할 작업
1. `models/flight.py` 작성 - Flight 모델
2. `models/carousel.py` 작성 - Carousel 모델
3. `models/assignment.py` 작성 - Assignment 모델
4. `models/airline.py` 작성 - Airline 모델
5. 테이블 자동 생성 확인
6. (선택) `/db-test` 임시 엔드포인트 삭제

### 현재 진행 상황
- **Phase 1 진행 중** (Backend 환경설정 및 DB 구축)
- Step 1.1 ~ 1.3 완료
- Step 1.4 (테이블 생성) 진행 예정

### 참고 사항
- PostgreSQL 접속: `psql -U postgres`
- Conda 환경 활성화: `conda activate betashift`
- 작업 디렉토리: `/Users/jisukim/projects/betashift/backend`
- 서버 실행: `uvicorn app.main:app --reload`

---

## 📁 프로젝트 구조 (예정)

```
alphashift/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── routers/
│   │   ├── services/
│   │   └── database.py
│   ├── requirements.txt
│   └── alembic/  (DB 마이그레이션)
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── services/
│   │   └── App.jsx
│   ├── package.json
│   └── vite.config.js
├── data/
│   └── sample_flights.json
├── docs/
│   ├── PROJECT.md
│   └── DEV_LOG.md
└── README.md
```

---

## 🔗 관련 자료

- **사업 배경**: Agentic AI 기반 공항 캐로셀/주기장 배정 자동화 소프트웨어
- **경쟁 분석**: Amadeus, SITA, Copenhagen Optimization 등 (현재 진정한 Agentic AI 기반 프로덕션 없음)
- **특허 분석**: 인천공항공사 특허 회피 설계 반영 (AI 기반 접근으로 차별화)

---

*최종 업데이트: 2025-12-08*
